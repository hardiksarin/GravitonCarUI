<UserControl x:Class="GravitonCar.Search"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GravitonCar"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"

             mc:Ignorable="d" 
             d:DesignHeight="760" d:DesignWidth="940" Background="#FF3399FF" >
    <ScrollViewer VerticalScrollBarVisibility="Hidden" >
        <Grid>
            <Grid x:Name="print">


                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="20"></ColumnDefinition>

                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="20"></RowDefinition>

                    <RowDefinition Height="*"></RowDefinition>
 

                    <RowDefinition Height="20"></RowDefinition>


                </Grid.RowDefinitions>
                <TextBlock Grid.Row="1" Margin="0,0,0,00" Grid.Column="1" Text="Search KYC"  HorizontalAlignment="Center" Foreground="White" VerticalAlignment="Top" FontSize="80" FontWeight="Light"></TextBlock>

                <Grid Grid.Row="1" Grid.Column="1"
                    Width="124" VerticalAlignment="Center">
                    <Button Foreground="#FF3399FF" Background="White"
                            Style="{StaticResource MaterialDesignRaisedDarkButton}"
                            Height="50"
                            materialDesign:ButtonAssist.CornerRadius="25"
                            ToolTip="Search KYC">
                        <TextBlock Text="Search" FontWeight="Light" />
                        </Button>   
                </Grid>     

            </Grid>
        </Grid>
    </ScrollViewer>

</UserControl>

